{
  "name": "dealduck",
  "version": "1.0.0",
  "description": "Trading platform with Steam integration",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:*\"",
    "dev:backend": "cd server && bun run dev",
    "dev:frontend": "cd client && npm run dev",
    "install:all": "npm install && cd server && bun install && cd ../client && npm install",
    "build:all": "cd server && bun run db:generate && cd ../client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
